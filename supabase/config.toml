project_id = "adtxjmqshckhmyziehpv"

# The Stripe webhook is a Netlify Function (netlify/functions/stripe-webhook.js),
# reachable at https://apptrainingmode.com/.netlify/functions/stripe-webhook.
# A second, Deno copy used to live under supabase/functions; it was removed so
# two implementations can never both grant Pro.
